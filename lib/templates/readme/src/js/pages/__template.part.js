/**
 * Use this file as a template for new page controller.
 * Copy this file and rename "__template" to anything you want to name.
 *
 * global $, jQuery
 */
"use strict";

import AppController from "../AppController";

export default class __TemplateController extends AppController {
  static get page() {
    return "__template";
  }
  
  constructor(){
    super();
  }
}
